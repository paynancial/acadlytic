# /cms/ — reserved for the future CMS

This directory is a placeholder only. The CMS and admin panel are a separate,
authenticated phase (see `docs/AUTH_ARCHITECTURE.md` and `docs/CMS_PHASE3_BLUEPRINT.md`).

- `/cms/index.php` returns a 404 until the CMS is built.
- `/cms/` is disallowed in `robots.txt`.
- Do not place credentials or unpublished content here.
