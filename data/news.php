<?php
/**
 * Company news for /company/news/. Newest first. Publish only real,
 * verifiable announcements. The page stays noindex until the first item is
 * added.
 *
 * Item keys: date (YYYY-MM-DD, required), title (required), summary
 * (required), url (optional internal path or external https URL).
 */
declare(strict_types=1);

return [
    // ['date' => '2026-10-01', 'title' => 'Announcement title', 'summary' => 'Two factual sentences.', 'url' => '/company/press/'],
];
